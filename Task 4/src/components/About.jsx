export default function About() {
  return (
    <section className="page">
      <h2>About Us</h2>
      <p>This React Router application demonstrates client-side routing without page reloads.</p>
      <h3>Key Features:</h3>
      <ul>
        <li>Single Page Application (SPA) with React Router</li>
        <li>Client-side navigation between pages</li>
        <li>Controlled forms with React hooks (useState)</li>
        <li>Form submission and validation</li>
        <li>Dynamic component rendering based on routes</li>
      </ul>
      <p>This application is built with React 18 and uses Vite as the build tool for fast development and optimized production builds.</p>
    </section>
  )
}
